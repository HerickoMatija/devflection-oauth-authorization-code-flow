import './Auth.css';

function Auth() {
    return (
        <div className='auth-container'>
            <button>
                Get the access token!
            </button>
        </div>
    );
}

export default Auth;